## Instalation
- Start up Docker
- Open the terminal
- Clone the repo
- Navigate to the repo folder

## Usage
- In the terminal inside the repo folder
- Run 'docker compose up'
- Open up your browser
- 'localhost:8080' for client side
- 'localhost:3000' for server side

## End Process
- Ctrl + c
- In terminal 'docker compose down'